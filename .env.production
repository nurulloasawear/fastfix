# Production build (Cloudflare Pages). Vite loads this for `vite build`.
# Hits the live backend; MSW never runs in a prod build (gated on import.meta.env.DEV).
VITE_API_BASE_URL=https://api.ozb.uz/api/v1
VITE_ENABLE_MSW=false

# Pre-launch access gate (client-side, 7-day localStorage). Plaintext never shipped.
VITE_GATE_PASSWORD_HASH=9339cc264f1d0b3d1b9b7f4a94b5eb7915df12a326b263cd63f3eac91d9e25c6
