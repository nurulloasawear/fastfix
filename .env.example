# Base URL of the OZB backend API (ozb-backend). No secrets here — only VITE_* public values.
VITE_API_BASE_URL=http://localhost:8080/api/v1

# When "true", start the MSW mock layer in dev so the UI runs without a live backend.
VITE_ENABLE_MSW=false
